<Page x:Class="WpfApp1.Result"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Result">

    <Grid Background="DarkBlue">
        <StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Показать полные ответы" Click="btnShowFullAnswers_Click" Margin="10"/>
                <Button Content="Выйти" Click="btnExit_Click" Margin="10"/>
            </StackPanel>
            <TextBlock Text="Результаты" Foreground="White" FontSize="24" HorizontalAlignment="Center" Margin="0,20,0,20"/>
            <TextBlock x:Name="txtResult" TextWrapping="Wrap" Foreground="White" FontSize="18" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>